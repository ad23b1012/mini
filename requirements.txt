# ============================================================
# Multimodal Emotion Recognition with XAI + NLG
# Requirements — tested with Python 3.10+ / CUDA 12.x
# ============================================================

# ---------- Core DL ----------
torch>=2.1.0
torchvision>=0.16.0
torchaudio>=2.1.0

# ---------- Transformers / NLP ----------
transformers>=4.36.0
tokenizers>=0.15.0

# ---------- Vision ----------
timm>=0.9.12                   # EfficientNet-B2 and other vision models
Pillow>=10.0.0

# ---------- XAI ----------
shap>=0.43.0                   # SHAP text explainer
captum>=0.7.0                  # Integrated gradients (optional)
pytorch-grad-cam>=1.5.0        # Grad-CAM implementation

# ---------- Data & Science ----------
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.11.0
scikit-learn>=1.3.0

# ---------- Visualization ----------
matplotlib>=3.7.0
seaborn>=0.12.0

# ---------- Config & Logging ----------
pyyaml>=6.0
tensorboard>=2.14.0
wandb>=0.15.0                  # Optional: Weights & Biases
tqdm>=4.65.0

# ---------- Video Processing (MELD) ----------
opencv-python>=4.8.0
decord>=0.6.0                  # Fast video reading

# ---------- NLG / LLM (optional) ----------
# Uncomment if using LLM-powered narration
# ollama>=0.1.0               # Local LLM via Ollama
# openai>=1.0.0               # OpenAI API (optional)

# ---------- Utilities ----------
requests>=2.31.0
gdown>=4.7.0                   # Google Drive downloads
colorama>=0.4.6
